1. R代表视频资源（Resource），L代表比特率等级（Level），图中有三种不同视频资源的三种不同等级
2. 云服务器含有所有资源的所有等级，边缘服务器选择性缓存某些资源的某些等级（见图中方框表示）
3. 每个用户只请求某个视频资源的某个等级
4. 每个边缘服务器有自己服务的用户组（例如E1服务于U1,U2,U3），对于用户请求的资源，如果边缘服务器缓存了指定资源则直接传输，否则需要从云服务器获取后传输（例如E1未缓存U2请求的内容，则从C1中获取并传输给U2）
5. 边缘服务器也可能缓存了未被请求的内容，例如E2缓存了R3的L1等级内容，可以视为是边缘服务器内存的浪费